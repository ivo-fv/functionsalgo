package functionalgo;

/**
 * Implementations of ExchangeTradeExecutor should have methods and fields such that they set some arbitrary actions to
 * then be executed via the execute() method.
 */
public interface ExchangeTradeExecutor {
    
    void execute();
}
