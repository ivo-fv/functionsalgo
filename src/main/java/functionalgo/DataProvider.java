package functionalgo;

/**
 * Implementations of DataProvider should grab whatever data the subclass is supposed to provide and make it
 * accessible via getter methods returning some chosen or purposely designed data type.
 */
public interface DataProvider {
    
}
